# Global configuration
spring.application.name=t2d2-authentication-ms

# Lazy Spring's Open Session In View (OSIV) configuration. 
spring.jpa.open-in-view=true

# Tomcat configuration
server.port=7071

# Eureka configuration
eureka.client.enabled=false

# Database configuration
spring.datasource.url=jdbc:h2:mem:test;DB_CLOSE_DELAY=-1;NON_KEYWORDS=KEY,VALUE
spring.datasource.username=mls_t2d2_practitioner_test
spring.datasource.password=mlst2d2practitionertest
spring.datasource.initialize=true

# Logging configuration
logging.level.root=ERROR
logging.level.org.springframework.boot.web.embedded.tomcat=INFO